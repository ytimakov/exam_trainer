# Инструкция по развертыванию в GitHub

## Подготовка проекта к публикации

### 1. Проверка .gitignore

Убедитесь, что `.gitignore` содержит следующие исключения:
- `secrets/` - папка с данными пользователей
- `secrets_config.json` - конфигурация Secret
- `venv/` - виртуальное окружение
- `__pycache__/` - кэш Python
- `*.pyc`, `*.pyo` - скомпилированные файлы

### 2. Проверка секретных данных

Перед коммитом убедитесь, что следующие файлы/папки НЕ попадут в репозиторий:
- ✅ `secrets/` - должна быть исключена
- ✅ `secrets_config.json` - должен быть исключен
- ✅ `venv/` - должна быть исключена
- ✅ Любые файлы с реальными Secret строками

### 3. Проверка файлов для коммита

Выполните проверку:
```bash
git status
```

Убедитесь, что в списке изменений НЕТ:
- `secrets/`
- `secrets_config.json`
- `venv/`

## Создание репозитория на GitHub

### Вариант 1: Через веб-интерфейс GitHub

1. Войдите в GitHub и нажмите **"New repository"**
2. Заполните данные:
   - **Repository name:** `exam_trainer`
   - **Description:** "Тренажёр для подготовки к экзаменам с системой авторизации"
   - **Visibility:** Public или Private (на ваше усмотрение)
   - **НЕ** добавляйте README, .gitignore или лицензию (они уже есть)
3. Нажмите **"Create repository"**

### Вариант 2: Через командную строку

Если репозиторий уже создан на GitHub:

```bash
# Инициализация Git (если еще не инициализирован)
git init

# Добавление всех файлов (кроме исключенных в .gitignore)
git add .

# Первый коммит
git commit -m "Initial commit: Exam trainer with authentication"

# Добавление удаленного репозитория (замените URL на ваш)
git remote add origin https://github.com/ваш-username/exam_trainer.git

# Переименование ветки в main (если нужно)
git branch -M main

# Отправка в GitHub
git push -u origin main
```

## Команды Git для работы с репозиторием

### Первая публикация

```bash
# Проверка статуса
git status

# Добавление файлов
git add .

# Коммит
git commit -m "Описание изменений"

# Отправка в GitHub
git push origin main
```

### Обновление репозитория

```bash
# Проверка изменений
git status

# Добавление измененных файлов
git add .

# Коммит
git commit -m "Описание изменений"

# Отправка обновлений
git push origin main
```

### Клонирование репозитория на другой машине

```bash
git clone https://github.com/ваш-username/exam_trainer.git
cd exam_trainer
pip install -r requirements.txt
python trainer_app.py
```

## Важные замечания

### ⚠️ Безопасность

1. **Никогда не коммитьте:**
   - Secret строки пользователей
   - `secrets_config.json` с реальными данными
   - Папку `secrets/` с прогрессом пользователей
   - `SECRET_KEY` из переменных окружения

2. **Проверка перед коммитом:**
   ```bash
   # Просмотр файлов, которые будут добавлены
   git status
   
   # Просмотр содержимого коммита
   git diff --cached
   ```

3. **Если случайно добавили секретные данные:**
   ```bash
   # Удаление из индекса (но сохранение локально)
   git rm --cached secrets_config.json
   git rm --cached -r secrets/
   
   # Добавление в .gitignore (если еще не добавлено)
   echo "secrets_config.json" >> .gitignore
   echo "secrets/" >> .gitignore
   
   # Коммит исправлений
   git add .gitignore
   git commit -m "Remove sensitive data from repository"
   ```

### 📝 Рекомендации

1. **Создайте файл `.env.example`** (опционально):
   ```
   SECRET_KEY=your-secret-key-here
   ```
   Это поможет другим разработчикам понять, какие переменные окружения нужны.

2. **Обновите README.md** с актуальной информацией о проекте.

3. **Добавьте лицензию** (если планируете открытый исходный код).

4. **Создайте Issues и Pull Requests** для отслеживания задач и улучшений.

## Структура для первого коммита

Убедитесь, что в репозитории есть:
- ✅ `trainer_app.py`
- ✅ `exam_editor_models.py`
- ✅ `generate_secret.py`
- ✅ `requirements.txt`
- ✅ `README.md`
- ✅ `.gitignore`
- ✅ `templates/` (все HTML файлы)
- ✅ `sources/` (конфигурация и примеры вопросов)
- ✅ `static/` (если есть статические файлы)
- ❌ `secrets/` (НЕ должно быть)
- ❌ `secrets_config.json` (НЕ должно быть)
- ❌ `venv/` (НЕ должно быть)

## Дополнительные файлы (опционально)

### LICENSE
Добавьте файл лицензии, если планируете открытый исходный код.

### .github/workflows/
Можно добавить GitHub Actions для автоматического тестирования или деплоя.

### CONTRIBUTING.md
Инструкции для контрибьюторов (если планируете принимать вклад).

